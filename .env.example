# Database (Local Postgres - e.g. Docker)
# docker run -d --name postgres-local -e POSTGRES_USER=admin -e POSTGRES_PASSWORD=admin123 -e POSTGRES_DB=mydb -v pgdata:/var/lib/postgresql/data -p 5432:5432 postgres:15
DATABASE_URL="postgresql://admin:admin123@localhost:5432/mydb"

# Auth (generate random strings for production)
JWT_SECRET="super_secret_dev_key_123"
COOKIE_NAME="menumate_session"

# Next.js
NODE_ENV="development"

# Razorpay (optional – for Pay online; set for production)
# RAZORPAY_KEY_ID=
# RAZORPAY_KEY_SECRET=
# NEXT_PUBLIC_RAZORPAY_KEY_ID=
# RAZORPAY_WEBHOOK_SECRET=  # From Dashboard → Webhooks → Secret (for payment.captured)
